from django.apps import AppConfig


class DayXConfig(AppConfig):
    """Configuration class for the 'network_live' app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'day_x'
