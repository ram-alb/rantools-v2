from django.apps import AppConfig


class EnmBulkConfigConfig(AppConfig):
    """Configuration for the ENM Bulk Config app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "enm_bulk_config"
