from django.apps import AppConfig


class RetsubunitConfig(AppConfig):
    """Configuration for the RetSubUnit app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "retsubunit"
