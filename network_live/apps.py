from django.apps import AppConfig


class NetworkLiveConfig(AppConfig):
    """Configuration class for the 'network_live' app."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'network_live'
