from django.apps import AppConfig


class NeighborsConfig(AppConfig):
    """Configuration class for the 'neighbors' app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'neighbors'
