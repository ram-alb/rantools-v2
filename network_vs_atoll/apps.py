from django.apps import AppConfig


class NetworkVsAtollConfig(AppConfig):
    """Configuration for the Network vs Atoll app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "network_vs_atoll"
